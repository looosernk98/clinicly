services:
  app:
    build: .
    image: "clinicly-image"
    container_name: "clinicly-container"
    restart: unless-stopped
    ports: 
      - "8000:8000"
    env_file:
    - .env
    volumes:
    - .:/app
    - /app/node_modules
    
    working_dir: /app
    command: npm run dev

